export enum State {
  REQUESTED = 'requested',
  INSPECTED = 'inspected',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  ACCEPTED = 'accepted',
  SELLER_CONF_PENDING = 'seller_confirmation_pending',
}
